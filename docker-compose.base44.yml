services:
  app:
    image: node:22-slim
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    command: sh -c "npm install && npx --yes nodemon server.js"
